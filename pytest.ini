[pytest]
testpaths =
    api/tests
    sdk/python/tests
    test/smoke
pythonpath =
    .
    sdk/python
markers =
    unit: unit tests
    integration: integration tests requiring an external stack
addopts = -m "not integration"
